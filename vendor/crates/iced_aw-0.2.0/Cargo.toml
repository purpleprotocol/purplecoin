# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "iced_aw"
version = "0.2.0"
authors = ["Kaiden42 <gitlab@tinysn.com>"]
description = "Additional widgets for the Iced GUI library"
readme = "README.md"
keywords = ["gui", "graphics", "interface", "widgets", "iced"]
categories = ["gui"]
license = "MIT"
repository = "https://github.com/kaiden42/iced_aw"
resolver = "2"
[profile.dev.package."*"]
opt-level = 2
[dependencies.iced_style]
version = "0.4.0"

[dependencies.num-traits]
version = "0.2.15"
optional = true

[features]
badge = []
button = []
card = []
color_picker = ["icon_text", "iced_graphics/canvas"]
colors = []
date_picker = ["chrono", "lazy_static", "icon_text"]
default = ["badge", "card", "colors", "date_picker", "color_picker", "floating_button", "floating_element", "grid", "modal", "pure", "tab_bar", "tabs", "time_picker", "wrap", "selection_list", "split"]
floating_button = ["button"]
floating_element = ["button", "floating_button"]
glow = []
grid = []
icon_text = ["icons"]
icons = []
modal = []
number_input = ["num-traits"]
pure = ["iced_pure"]
selection_list = []
split = []
tab_bar = []
tabs = ["tab_bar"]
time_picker = ["chrono", "icon_text", "iced_graphics/canvas"]
wrap = []
[target."cfg(not(target_arch = \"wasm32\"))".dependencies.chrono]
version = "0.4.19"
optional = true

[target."cfg(not(target_arch = \"wasm32\"))".dependencies.iced_graphics]
version = "0.3.0"

[target."cfg(not(target_arch = \"wasm32\"))".dependencies.iced_native]
version = "0.5.1"

[target."cfg(not(target_arch = \"wasm32\"))".dependencies.iced_pure]
version = "0.2.2"
optional = true

[target."cfg(not(target_arch = \"wasm32\"))".dependencies.lazy_static]
version = "1.4.0"
optional = true
